name: CI/CD Pipeline

on:
  push:
    branches:
      - '**'
  pull_request:
  repository_dispatch:
    types: [cms-update]
  schedule:
    #atualizar isto depois no fim, antes da entrega
    - cron: '0 0 * * 1' # todos os dias às 00:00 UTC

jobs:
  build-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar dependências
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Testes
        run: npm test

  build:
    needs: build-and-validate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar dependências
        run: npm ci

  deploy:
    runs-on: ubuntu-latest
    needs: build # garante que o build passa antes
    if: github.ref == 'refs/heads/main'
    env:
      CONTENTFUL_SPACE_ID: ${{ secrets.CONTENTFUL_SPACE_ID }}
      CONTENTFUL_ACCESS_TOKEN: ${{ secrets.CONTENTFUL_ACCESS_TOKEN }}
      CONTENTFUL_PREVIEW_ACCESS_TOKEN: ${{ secrets.CONTENTFUL_PREVIEW_ACCESS_TOKEN }}
      TOKEN_GITHUB: ${{ secrets.TOKEN_GITHUB }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build 2
        run: npm run build

      - name: Deploy to Netlify
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        run: npx netlify-cli deploy --site $NETLIFY_SITE_ID --auth $NETLIFY_AUTH_TOKEN --prod

      - name: Notificação no Microsoft Teams (Sucesso)
        if: success()
        run: |
          DEPLOY_URL="https://tdw-mp1-leandro-oliveira.tdw-mctw.pt"
          COMMIT_MESSAGE=$(git log -1 --pretty=%s)
          COMMIT_AUTHOR=$(git log -1 --pretty=%an)
          WORKFLOW_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          PAYLOAD=$(jq -n \
            --arg text "✅ Deploy concluído com sucesso no Netlify! 🎉" \
            --arg author "$COMMIT_AUTHOR" \
            --arg msg "$COMMIT_MESSAGE" \
            --arg url "$WORKFLOW_URL" \
            --arg deploy "$DEPLOY_URL" \
            '{text: "\($text)\n\n🧑‍💻 Autor: \($author)\n📝 Commit: \($msg)\n🔗 [Ver pipeline no GitHub Actions](\($url))\n🌐 [Ver site publicado](\($deploy))"}')

          curl -H 'Content-Type: application/json' \
               -d "$PAYLOAD" \
               ${{ secrets.TEAMS_WEBHOOK_URL }}

      - name: Notificação no Microsoft Teams (Falha)
        if: failure()
        run: |
          COMMIT_MESSAGE=$(git log -1 --pretty=%s)
          COMMIT_AUTHOR=$(git log -1 --pretty=%an)
          WORKFLOW_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          PAYLOAD=$(jq -n \
            --arg text "❌ O deploy falhou no GitHub Actions! 😞" \
            --arg author "$COMMIT_AUTHOR" \
            --arg msg "$COMMIT_MESSAGE" \
            --arg url "$WORKFLOW_URL" \
            '{text: "\($text)\n\n🧑‍💻 Autor: \($author)\n📝 Commit: \($msg)\n🔗 [Ver pipeline no GitHub Actions](\($url))"}')

          curl -H 'Content-Type: application/json' \
               -d "$PAYLOAD" \
               ${{ secrets.TEAMS_WEBHOOK_URL }}
